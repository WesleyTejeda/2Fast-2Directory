import React from "react";
import styles from "./style.css";

export default function Nav() {
    return (
        <div className="container headerDiv">
            <h2 className="text-center">2 Fast 2 Furious Directory</h2>
            <h4>Search by any field to minimize your results. If your search only returns one character then you will see some extra information regarding them.</h4>
        </div>
    )
}